default:
  description: 'Default (production) build'
  tasks:
    - 'jshint'
    - 'uglify'
    - 'cssmin'
    - 'copy:index'

img:
  description: 'Image tasks'
  tasks:
    - 'imagemin'

data:
  description: 'Copy json data, minify inplace, then rename'
  tasks:
    - 'copy:data'
    - 'json-minify'
